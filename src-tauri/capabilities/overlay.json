{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Battery-event overlay windows — minimal: they only destroy themselves.",
  "windows": ["battery-overlay-*"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
